<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Add Product</title>
  <style>
    * {
      box-sizing: border-box;
      font-family: "Segoe UI", Tahoma, sans-serif;
    }

    body {
      margin: 0;
      padding: 40px;
      background: #f4f6f9;
    }

    .container {
      max-width: 600px;
      margin: auto;
      background: #fff;
      padding: 30px 35px;
      border-radius: 14px;
      box-shadow: 0 15px 35px rgba(0,0,0,0.1);
    }

    h2 {
      margin-bottom: 25px;
      color: #333;
      letter-spacing: 1px;
      text-align: center;
    }

    .form-group {
      margin-bottom: 18px;
    }

    .form-group label {
      display: block;
      margin-bottom: 6px;
      font-size: 14px;
      font-weight: 600;
      color: #555;
    }

    .form-group input,
    .form-group textarea {
      width: 100%;
      padding: 11px 14px;
      border-radius: 8px;
      border: 1px solid #ddd;
      font-size: 14px;
      outline: none;
      transition: 0.3s;
    }

    .form-group textarea {
      resize: vertical;
      min-height: 80px;
    }

    .form-group input:focus,
    .form-group textarea:focus {
      border-color: #667eea;
      box-shadow: 0 0 0 3px rgba(102,126,234,0.15);
    }

    .btn-group {
      display: flex;
      gap: 12px;
      margin-top: 25px;
    }

    .btn {
      flex: 1;
      padding: 12px;
      border-radius: 8px;
      border: none;
      font-size: 15px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    .btn-save {
      background: linear-gradient(135deg, #4facfe, #00f2fe);
      color: white;
    }

    .btn-back {
      background: #eee;
      color: #555;
      text-decoration: none;
      text-align: center;
      line-height: 44px;
    }

    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 15px rgba(0,0,0,0.15);
    }
  </style>
</head>

<body>

<div class="container">
  <h2>➕ Add New Product</h2>

  <form method="post" action="/products/add">
    <div class="form-group">
      <label>Model</label>
      <input name="model" placeholder="Product model" required />
    </div>

    <div class="form-group">
      <label>Description</label>
      <textarea name="description" placeholder="Product description"></textarea>
    </div>

    <div class="form-group">
      <label>Quantity</label>
      <input type="number" name="quantity" placeholder="Quantity" required />
    </div>

    <div class="form-group">
      <label>Price</label>
      <input type="number" step="0.01" name="price" placeholder="Price" required />
    </div>

    <div class="form-group">
      <label>Image URL</label>
      <input name="imgurl" placeholder="https://..." />
    </div>

    <div class="btn-group">
      <button class="btn btn-save" type="submit">💾 Save</button>
      <a class="btn btn-back" href="/products">⬅ Back</a>
    </div>
  </form>
</div>

</body>
</html>
